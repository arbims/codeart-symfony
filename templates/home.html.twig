{% extends 'base.html.twig' %}

{% block title 'Home' %}

{% block body %}
   

<div class="hero_user">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <p><span id="typed-text" class="typed_element" data-annimed="false"></span></p>

                <p class="hero_user_bold"><span class="custom_text_hero">Améliorez-vous</span> et apprenez de nouvelles choses </p>
                <a href="#" class="btn btn-primary hero_button">Voir les tutoriels</a>
               
                <a href="#" class="btn btn-primary hero_button hero_button_custom">Crée Vote Compte</a>
                
            </div>
            <div class="col-md-6">
                <img src="/img/web.png" class="hero_image" alt="">
            </div>
        </div>
    </div>
</div>

<div class="container" style="margin-top:50px;">
        {% for post in posts %}
        <div class="article">
            <div class="row">
                <div class="col-md-4">
                    <a href="">
                        <img src="{{ asset(post.imageFile)}}" alt="{{ post.image}}" "class" = "artticle-img">
                    </a>
                </div>
                <div class="col-md-8">
                    <h2><a href="" class="blog-link">{{ post.name }}</a></h2>
                    <div class="article-date">crée le :  par : </div>
                    <p></p>

                </div>
            </div>
        {% endfor %}
        </div>
</div>

{% endblock %}